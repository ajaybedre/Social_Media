var express = require('express');
var router = express.Router();
var connection  = require('../app_sql');
//const { to } = require('./book');
var del = require('../../Try.js');

function openit()
{
    console.log(del.sender, del.reciever);   
}